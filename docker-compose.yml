version: '3.3'

services:
  # ---------------------------
  # Team Three
  # ---------------------------
  team-three:
    container_name: "team-three"
    hostname: "team-three"
    build: .
    image: team-three
    networks:
      - micro-frontend
networks:
  micro-frontend:
    driver: bridge